export interface curriculo {
  // Defina os campos conforme necessário, por exemplo:
  id: number;
  nome: string;
  email: string;
  foto?: string;
  // Adicione outros campos conforme o seu modelo
}